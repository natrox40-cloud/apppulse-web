export const metadata = { title: "AppPulse — App Health Monitor", description: "AI-powered weekly app health reports" };
export default function RootLayout({ children }) {
  return <html lang="ko"><body style={{ margin: 0, background: "#0e0e0e" }}>{children}</body></html>;
}
